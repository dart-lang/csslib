name: csslib
version: 0.17.0

description: A library for parsing CSS.
repository: https://github.com/dart-lang/csslib

environment:
  sdk: '>=2.12.0-0 <3.0.0'

dependencies:
  source_span: ^1.8.0

dev_dependencies:
  pedantic: ^1.10.0
  test: ^1.16.0
