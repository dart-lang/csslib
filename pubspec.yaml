name: csslib
version: 0.16.2-dev

description: A library for parsing CSS.
homepage: https://github.com/dart-lang/csslib

environment:
  sdk: '>=2.2.0 <3.0.0'

dependencies:
  source_span: ^1.4.0

dev_dependencies:
  pedantic: ^1.0.0
  test: ^1.2.0
